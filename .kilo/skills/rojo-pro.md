# Rojo 专家技能

## 核心命令

```bash
rojo init              # 初始化项目
rojo serve             # 启动开发服务器
rojo build -o game.rbxl # 构建可发布文件
rojo sourcemap         # 生成类型检查用的 sourcemap
rojo plugin install    # 安装 Studio 插件
```
